name: Pull request labeler
on: 
  schedule:
    - corn: 

permissions:
  contents: read
  pull-requests: write

jobs:
  triage:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/labeler@v4
        with:
          repo-token: ${{ secrets.CANDY_SHOP }}
